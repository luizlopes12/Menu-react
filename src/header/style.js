import styled from "styled-components";

export const Header = styled.header`

`
export const Nav = styled.nav`
    position: fixed;
    top: 0;
    width: 100%;
`;
export const Nav = styled.nav`
    position: fixed;

`;
export const Nav = styled.nav`
    position: fixed;

`;
export const Nav = styled.nav`
    position: fixed;

`;
export const Nav = styled.nav`
    position: fixed;

`;
